query getPost($id: String!) {
  post(id: $id) {
    id
    title
    body
    user {
      id
      name
    }
  }
}

query getPosts {
  posts {
    id
    title
    body
    user {
      id
      name
    }
  }
}
