{
  "name": "nest-firestore-graphql",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "license": "UNLICENSED",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "prepare": "node -e 'process.exit(process.env.NODE_ENV === `production` ? 0 : 1)' || husky install"
  },
  "lint-staged": {
    "*.{js,cjs,ts,json,svelte}": "prettier --write",
    "*.{js,ts,svelte}": "eslint --max-warnings 0 --fix",
    "*.svelte": "svelte-check --fail-on-warnings --fail-on-hints"
  },
  "devDependencies": {
    "husky": "^6.0.0",
    "lint-staged": "^10.5.4"
  }
}
